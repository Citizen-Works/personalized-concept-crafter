
import { useMarketingExamplesAdapter } from './api/adapters/useMarketingExamplesAdapter';

/**
 * @deprecated Use useMarketingExamplesApi or useMarketingExamplesAdapter instead
 * This hook is kept for backward compatibility
 */
export const useMarketingExamples = useMarketingExamplesAdapter;
